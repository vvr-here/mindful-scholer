# -----------------------------------------------------------------------------
# FRONTEND ENVIRONMENT VARIABLES (DEVELOPMENT / LOCAL)
# Copy this file to '.env' so Vite can load it.
# -----------------------------------------------------------------------------

# For local development, we proxy `/api` via vite.config.js, 
# so we can point the base url locally.
VITE_API_BASE_URL="http://localhost:4000/api"
