# -----------------------------------------------------------------------------
# FRONTEND ENVIRONMENT VARIABLES (PRODUCTION)
# In Vercel (or your hosting provider), add this exact key-value pair 
# to the project's Environment Variables settings.
# -----------------------------------------------------------------------------

# Point directly to your deployed backend URL. No trailing slash!
VITE_API_BASE_URL="https://your-production-backend-url.com/api"
